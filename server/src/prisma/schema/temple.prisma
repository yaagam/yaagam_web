model Temple {
  id        String    @id @default(uuid())
  name      String
  imageKey  String?
  district  String
  place     String
  createdAt DateTime  @default(now())
  updatedAt DateTime  @updatedAt
  bookings  Booking[]
  poojas    Pooja[]
}
